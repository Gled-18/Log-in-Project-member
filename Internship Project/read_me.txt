Firstly copy paste sql queries in database in mysql server in order to create database for the project.
Hashed password for admin user is admin, for user is user.
The only way to create admin or simple users is by an admin account.
Admin can create users, view and delete all users, can check and delete all project but cannot change them.
Only an admin for that project can delete, edit or add members.
An user is responsible only for his projects that is admin. He can view all projects but cannot do anything to them.